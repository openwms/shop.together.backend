spring:
  datasource:
    url: jdbc:postgresql:S2GDB
    username: S2G
    password: S2G
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
